nav(class="navbar navbar-expand-lg navbar-light bg-light")
  a(class="navbar-brand", href="#") DuySmile
  button(class="navbar-toggler", type="button", data-toggle="collapse", data-target="#navbarNavAltMarkup", aria-controls="navbarNavAltMarkup", aria-expanded="false", aria-label="Toggle navigation")
    span(class="navbar-toggler-icon")
  div(class="collapse navbar-collapse justify-content-between", id="navbarNavAltMarkup")
    div(class="navbar-nav")
      if user && isAdmin
        a(class="nav-item nav-link", href="/books/list") Books 
        a(class="nav-item nav-link", href="/users") Users
      if user && !isAdmin
        a(class="nav-item nav-link", href="/transactions") Transactions
      if user && user.shop        
        a(class="nav-item nav-link", href="/books/create") Add book
      
      if !isAdmin
        a(class="nav-item nav-link", href="/books") Books 
      
      a(class="nav-item nav-link", href="/cart")= 'Cart(' + cartCount + ')'
      
      if user && !isAdmin
        a(class="nav-item nav-link", href="/transactions/add") Borrow
        if (!user.shop)
          a(class="nav-item nav-link", href="/shops/create") Create shop
    if user
      div= user.name
    else 
      a(class="nav-item nav-link", href="/auth/login") Login 